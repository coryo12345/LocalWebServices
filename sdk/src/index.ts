import { Result } from "./common/models";
import PropertyManager, { Property } from "./property_manager";
import FileStorage, { FileMeta } from "./file_storage";

export { PropertyManager, FileStorage };
export type { Property, FileMeta, Result };
